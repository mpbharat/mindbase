name = "brain-worker"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

account_id = "YOUR_CLOUDFLARE_ACCOUNT_ID"

# Custom route — enable after YOUR_DOMAIN.com is added to Cloudflare
# [[routes]]
# pattern = "brain.YOUR_DOMAIN.com/*"
# zone_name = "YOUR_DOMAIN.com"

[vars]
ENVIRONMENT = "production"

[ai]
binding = "BRAIN_AI"
